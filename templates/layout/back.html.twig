{% extends 'base.html.twig' %}
{% block header  %}
    <header>
        <div class="p-2 container">
            <nav class="navbar text-right navbar-expand-lg navbar-light bg-light">

                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarSupportedContent">
                        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                            <li class="nav-item">
                                <a class="nav-link active" aria-current="accueil" href="{{ path('home') }}">Home</a>
                            </li>

                            <li class="nav-item">
                                <a class="nav-link active" aria-current="home admin" href="{{ path('admin') }}">Home Administration</a>
                            </li>


                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="{{path('admin')}}" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    Administration
                                </a>
                                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                    <li>
                                        <a class="dropdown-item" href="{{ path('admin_proprietaire') }}">Proprietaire</a>
                                    </li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li>
                                        <a class="dropdown-item" href="{{ path('admin_post_category') }}">Post Category</a>
                                    </li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li>
                                        <a class="dropdown-item" href="{{ path('admin_post') }}">Post</a>
                                    </li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li>
                                        <a class="dropdown-item" href="{{ path('admin_device') }}">Device</a>
                                    </li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li>
                                        <a class="dropdown-item" href="{{ path('admin_game_category') }}">Game Category</a>
                                    </li>
                                    <li><hr class="dropdown-divider"></li>

                                    <li>
                                        <a class="dropdown-item" href="{{ path('admin_game') }}">Game</a>
                                    </li>

                                    <li><hr class="dropdown-divider"></li>

                                    <li>
                                        <a class="dropdown-item" href="{{ path('admin_forum') }}">Forum</a>
                                    </li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li>
                                        <a class="dropdown-item" href="{{ path('admin_contact') }}">Message de contact</a>
                                    </li>
                                </ul>
                            </li>
                        </ul>
                    </div>


                <h5 class="text-end">
                    {% set nbMessageNomLu = nb_message_non_lu() %}
                    {% if nbMessageNomLu != 0 %}

                        {{ nbMessageNomLu }} notification(s) <i class="far fa-bell" type="button" data-bs-toggle="modal" data-bs-target="#notificationModal">
                        </i>
                    &nbsp;&nbsp;

                        <div class="modal fade" id="notificationModal" tabindex="-1" aria-labelledby="notificationModalLabel" aria-hidden="true">
                            <div class="modal-dialog">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        <h5 class="modal-title" id="notificationModalLabel"> {{ nbMessageNomLu  }} Notification(s)</h5>
                                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                    </div>
                                    <div class="modal-body text-start">
                                        {{  generate_notification_html()|raw}}
                                    </div>

                                </div>
                            </div>
                        </div>


                    {% endif %}


                    Bienvenue {{ app.user.firstName }}  {{ app.user.lastName }} <img width="5%" class="text-center" src="{{ asset('/uploads/avatar/OIP.jpg') }}" /> - <a href="{{ path('app_logout') }}"> Déconnexion</a></h5>
            </nav>

        </div>
    </header>
{% endblock %}
{% block footer  %}
{% endblock %}