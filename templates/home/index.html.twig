{% extends 'layout/front.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
    <div class="container">
    <h1 class="text-center">Bienvenue sur mon site </h1>

    <div class="container">
        <div class="row">
            <div class="card m-1 border-success col-3">
                <div class="card-body">
                    <h5 class="card-title text-center">Forum</h5>
                    <a href="{{ path('forum_list') }}"><img width="75%" class="text-center" src="{{ asset('/build/image/forum.png') }}" /></a>
                </div>
            </div>

            <div class="card m-1 border-success col-3">
                <div class="card-body">
                    <h5 class="card-title text-center">Post</h5>
                    <a href="{{ path('post') }}"><img width="75%" class="text-center" src="{{ asset('/build/image/post.png') }}" /></a>
                </div>
            </div>
            {% if app.user is not null %}
                <div class="card m-1 border-success col-3">
                    <div class="card-body">
                        <h5 class="card-title text-center">Nous contacter</h5>
                        <a href="{{ path('contact') }}"><img width="75%" class="text-center" src="{{ asset('/build/image/contact.jpg') }}" /></a>
                    </div>
                </div>
                {% if is_granted('ROLE_WORKER') == true %}
                    <div class="m-1 card border-success col-3">
                        <div class="card-body">
                            <h5 class="card-title text-center">Administration</h5>
                            <a href="{{ path('admin') }}"><img width="75%" class="text-center" src="{{ asset('/build/image/administration.png') }}" /></a>
                        </div>
                    </div>
                {% endif %}
            {% endif %}

        </div>
    </div>



    </div>
{% endblock %}
