{% extends 'layout/back.html.twig' %}
{% block title %}Update a new Device {% endblock %}
{% block body %}

    <div class="container">
        <a class="btn btn-primary" href="{{ path('admin_device') }}">Retour</a>
        <div class="m-2 container card">
            {{ form_start(form) }}
            <div class="my-custom-class-for-errors">
                {{ form_errors(form) }}
            </div>

            <div class="row p-2">
                <div class="col">
                    {{ form_row(form.name) }}
                </div>
            </div>
            <div class="row p-2">
                <div class="col-12 m-2">
                    {% if logo != '' %}
                        <img src="{{ asset('uploads/logo/' ~ logo) }}" />
                    {%endif %}
                    {{ form_label(form.pathLogo) }}
                    {{ form_widget(form.pathLogo) }}
                </div>
            </div>

            <div class="row p-2">
                <div class="col">
                    {{ form_row(form.submit, {'attr': {'class': 'btn btn-primary'}, 'label': 'Update' }) }}
                </div>
            </div>

            {{ form_end(form) }}
        </div>
    </div>


{% endblock %}
